<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.number().nil? -%><%== render :template => 'unsignedint', :locals => {name: 'number', model: model.number()} %><%- end -%>
    <%- if !model.uid().nil? -%><uid value="<%= model.uid() %>"/><%- end -%>
    <%- if !model.sopclass().nil? -%><sopclass value="<%= model.sopclass() %>"/><%- end -%>
    <%- if !model.fhirType().nil? -%><type value="<%= model.fhirType() %>"/><%- end -%>
    <%- if !model.title().nil? -%><title value="<%= model.title() %>"/><%- end -%>
    <%- if (!model.content().nil? && !model.content.empty?) -%><%- model.content().each do |element| -%><%== render :template => 'attachment', :locals => {name: 'content', model: element} %><%- end -%><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
