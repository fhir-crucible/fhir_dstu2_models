<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.summary().nil? -%><%== render :template => 'codeableconcept', :locals => {name: 'summary', model: model.summary()} %><%- end -%>
    <%- if (!model.assessment().nil? && !model.assessment.empty?) -%><%- model.assessment().each do |element| -%><%== render :template => 'reference', :locals => {name: 'assessment', model: element} %><%- end -%><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
