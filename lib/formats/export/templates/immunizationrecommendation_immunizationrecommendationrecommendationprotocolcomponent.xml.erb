<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.doseSequence().nil? -%><doseSequence value="<%= model.doseSequence %>"<%- if model.has_primitive_extension?('doseSequence') -%>><%== model.get_primitive_extension('doseSequence').to_xml(is_root: false, is_lowercase: true)  %></doseSequence><%- else -%>/><%- end -%><%- end -%>
    <%- if !model.description().nil? -%><description value="<%= model.description %>"<%- if model.has_primitive_extension?('description') -%>><%== model.get_primitive_extension('description').to_xml(is_root: false, is_lowercase: true)  %></description><%- else -%>/><%- end -%><%- end -%>
    <%- if !model.authority().nil? -%><%== render :template => 'reference', :locals => {name: 'authority', model: model.authority()} %><%- end -%>
    <%- if !model.series().nil? -%><series value="<%= model.series %>"<%- if model.has_primitive_extension?('series') -%>><%== model.get_primitive_extension('series').to_xml(is_root: false, is_lowercase: true)  %></series><%- else -%>/><%- end -%><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
