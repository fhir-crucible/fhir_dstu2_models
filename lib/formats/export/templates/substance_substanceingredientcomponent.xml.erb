<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.quantity().nil? -%><%== render :template => 'ratio', :locals => {name: 'quantity', model: model.quantity()} %><%- end -%>
    <%- if !model.substance().nil? -%><%== render :template => 'reference', :locals => {name: 'substance', model: model.substance()} %><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
