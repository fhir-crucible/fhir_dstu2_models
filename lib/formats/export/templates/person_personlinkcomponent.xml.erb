<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.other().nil? -%><%== render :template => 'reference', :locals => {name: 'other', model: model.other()} %><%- end -%>
    <%- if !model.assurance().nil? -%><assurance value="<%= model.assurance() %>"/><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
