<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if (!model.frameNumbers().nil? && !model.frameNumbers.empty?) -%><%- model.frameNumbers().each do |element| -%><%== render :template => 'unsignedint', :locals => {name: 'frameNumbers', model: element} %><%- end -%><%- end -%>
    <%- if !model.url().nil? -%><url value="<%= model.url() %>"/><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
