<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if (model.daysOfWeek() && !model.daysOfWeek.empty?) -%><%- model.daysOfWeek().each do |element| -%><daysOfWeek value="<%= element %>"/><%- end -%><%- end -%>
    <%- if !model.allDay().nil? -%><allDay value="<%= model.allDay() %>"/><%- end -%>
    <%- if !model.availableStartTime().nil? -%><availableStartTime value="<%= model.availableStartTime().to_time.strftime("%T") %>"/><%- end -%>
    <%- if !model.availableEndTime().nil? -%><availableEndTime value="<%= model.availableEndTime().to_time.strftime("%T") %>"/><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
