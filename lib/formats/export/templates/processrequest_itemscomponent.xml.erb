<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.sequenceLinkId().nil? -%><sequenceLinkId value="<%= model.sequenceLinkId %>"<%- if model.has_primitive_extension?('sequenceLinkId') -%>><%== model.get_primitive_extension('sequenceLinkId').to_xml(is_root: false, is_lowercase: true)  %></sequenceLinkId><%- else -%>/><%- end -%><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
