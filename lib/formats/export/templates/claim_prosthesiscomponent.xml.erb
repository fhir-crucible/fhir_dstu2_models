<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.initial().nil? -%><initial value="<%= model.initial() %>"/><%- end -%>
    <%- if !model.priorDate().nil? -%><%- if (!model.priorDate().match(/\A[0-9]{4}(-(0[1-9]|1[0-2])(-(0[0-9]|[1-2][0-9]|3[0-1]))?)?\Z/).nil?) -%><priorDate value="<%= model.priorDate %>"/><%- end -%><%- end -%>
    <%- if !model.priorMaterial().nil? -%><%== render :template => 'coding', :locals => {name: 'priorMaterial', model: model.priorMaterial()} %><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
