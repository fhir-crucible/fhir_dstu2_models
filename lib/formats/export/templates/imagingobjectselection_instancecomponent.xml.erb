<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.sopClass().nil? -%><sopClass value="<%= model.sopClass() %>"/><%- end -%>
    <%- if !model.uid().nil? -%><uid value="<%= model.uid() %>"/><%- end -%>
    <%- if !model.url().nil? -%><url value="<%= model.url() %>"/><%- end -%>
    <%- if (!model.frames().nil? && !model.frames.empty?) -%><%- model.frames().each do |element| -%><%== render :template => 'imagingobjectselection_framescomponent', :locals => {name: 'frames', model: element} %><%- end -%><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
