<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.sequenceLinkId().nil? -%><sequenceLinkId value="<%= model.sequenceLinkId %>"<%- if model.has_primitive_extension?('sequenceLinkId') -%>><%== model.get_primitive_extension('sequenceLinkId').to_xml(is_root: false, is_lowercase: true)  %></sequenceLinkId><%- else -%>/><%- end -%><%- end -%>
    <%- if (model.noteNumber && !model.noteNumber.empty?) -%><%- model.noteNumber.each_with_index do |element,index| -%><noteNumber value="<%= element %>"<%- if model.has_primitive_extension?('noteNumber',index) -%>><%== model.get_primitive_extension('noteNumber',index).to_xml(is_root: false, is_lowercase: true)  %></noteNumber><%- else -%>/><%- end -%><%- end -%><%- end -%>
    <%- if (!model.adjudication().nil? && !model.adjudication.empty?) -%><%- model.adjudication().each do |element| -%><%== render :template => 'claimresponse_itemadjudicationcomponent', :locals => {name: 'adjudication', model: element} %><%- end -%><%- end -%>
    <%- if (!model.detail().nil? && !model.detail.empty?) -%><%- model.detail().each do |element| -%><%== render :template => 'claimresponse_itemdetailcomponent', :locals => {name: 'detail', model: element} %><%- end -%><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
