<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if (!model.link().nil? && !model.link.empty?) -%><%- model.link().each do |element| -%><%== render :template => 'bundle_bundlelinkcomponent', :locals => {name: 'link', model: element} %><%- end -%><%- end -%>
    <%- if !model.fullUrl().nil? -%><fullUrl value="<%= model.fullUrl %>"<%- if model.has_primitive_extension?('fullUrl') -%>><%== model.get_primitive_extension('fullUrl').to_xml(is_root: false, is_lowercase: true)  %></fullUrl><%- else -%>/><%- end -%><%- end -%>
    <%- if !model.resource.nil? -%><resource><%== model.resource.to_xml(is_root: false)%></resource><%- elsif !model["resource".to_sym].nil? -%><resource><%== model["resource".to_sym].to_xml(is_root: false) %></resource><%- end -%>
    <%- if !model.search().nil? -%><%== render :template => 'bundle_bundleentrysearchcomponent', :locals => {name: 'search', model: model.search()} %><%- end -%>
    <%- if !model.request().nil? -%><%== render :template => 'bundle_bundleentryrequestcomponent', :locals => {name: 'request', model: model.request()} %><%- end -%>
    <%- if !model.response().nil? -%><%== render :template => 'bundle_bundleentryresponsecomponent', :locals => {name: 'response', model: model.response()} %><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
