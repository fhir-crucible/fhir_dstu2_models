<%== '<?xml version="1.0" encoding="UTF-8"?>' if is_root %>
<% local_name = name || 'Identifier' %>
<<%= (is_lowercase) ? local_name.downcase : local_name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
<%== (is_root) ? ' xmlns="http://hl7.org/fhir"' : ''%>>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.use().nil? -%><use value="<%= model.use() %>"/><%- end -%>
    <%- if !model.fhirType().nil? -%><%== render :template => 'codeableconcept', :locals => {name: 'type', model: model.fhirType()} %><%- end -%>
    <%- if !model.system().nil? -%><system value="<%= model.system() %>"/><%- end -%>
    <%- if !model.value().nil? -%><value value="<%= model.value() %>"/><%- end -%>
    <%- if !model.period().nil? -%><%== render :template => 'period', :locals => {name: 'period', model: model.period()} %><%- end -%>
    <%- if !model.assigner().nil? -%><%== render :template => 'reference', :locals => {name: 'assigner', model: model.assigner()} %><%- end -%>
</<%= (is_lowercase) ? local_name.downcase : local_name %>>
