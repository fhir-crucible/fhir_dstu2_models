<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.system().nil? -%><system value="<%= model.system() %>"/><%- end -%>
    <%- if !model.versionNum().nil? -%><version value="<%= model.versionNum() %>"/><%- end -%>
    <%- if (!model.concept().nil? && !model.concept.empty?) -%><%- model.concept().each do |element| -%><%== render :template => 'valueset_conceptreferencecomponent', :locals => {name: 'concept', model: element} %><%- end -%><%- end -%>
    <%- if (!model.filter().nil? && !model.filter.empty?) -%><%- model.filter().each do |element| -%><%== render :template => 'valueset_conceptsetfiltercomponent', :locals => {name: 'filter', model: element} %><%- end -%><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
