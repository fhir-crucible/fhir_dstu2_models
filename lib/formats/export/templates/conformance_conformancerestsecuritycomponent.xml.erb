<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model, is_resource: false} %>
    <%- if !model.cors().nil? -%><cors value="<%= model.cors %>"<%- if model.has_primitive_extension?('cors') -%>><%== model.get_primitive_extension('cors').to_xml(is_root: false, is_lowercase: true)  %></cors><%- else -%>/><%- end -%><%- end -%>
    <%- if (!model.service().nil? && !model.service.empty?) -%><%- model.service().each do |element| -%><%== render :template => 'codeableconcept', :locals => {name: 'service', model: element} %><%- end -%><%- end -%>
    <%- if !model.description().nil? -%><description value="<%= model.description %>"<%- if model.has_primitive_extension?('description') -%>><%== model.get_primitive_extension('description').to_xml(is_root: false, is_lowercase: true)  %></description><%- else -%>/><%- end -%><%- end -%>
    <%- if (!model.certificate().nil? && !model.certificate.empty?) -%><%- model.certificate().each do |element| -%><%== render :template => 'conformance_conformancerestsecuritycertificatecomponent', :locals => {name: 'certificate', model: element} %><%- end -%><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
